specVersion: "v1"
serviceId: "ai-trip-planner-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ai-trip-planner-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
